---
title: "Props"
description: "Solid Notifications props guide"
slug: "/props"
order: 3
tags: []
---

import Code, { InlineCode } from "~/components/Code";
import Title from "~/components/Title";

<Title>Props</Title>

# Props

<div />

## Toaster and ToastProvider only props {{ "data-nav": "link", "id": "toaster-provider-props" }}

Can be applied to `ToastProvider` and `Toaster`.

- **limit** - The maximum number of toasts to display at once. Excess toasts are queued and rendered when space becomes available. For no limit, set limit to `false` or `0`. {{ "class" : "mb-2",  "data-nav": "sublink", "id": "limit" }}

     <InlineCode lang="tsx">

  ```tsx
  limit: number | false;
  ```

     </InlineCode>

  Default value: `8` {{ "class": "mt-3 text-xs" }}

- **reverseToastOrder** - If true, toasts are rendered in reverse order (newest at the top/bottom). {{ "class" : "mb-2", "data-nav": "sublink", "id": "reverseToastOrder" }}

    <InlineCode lang="tsx">

        ```tsx
        reverseToastOrder: boolean;
        ```

    </InlineCode>

  Default value: `false` {{ "class": "mt-3 text-xs" }}

- **offsetX** - Horizontal offset for the toaster's position. {{ "class" : "mb-2", "data-nav": "sublink", "id": "offsetX" }}

        <InlineCode lang="tsx">

            ```tsx
            offsetX: number;
            ```

        </InlineCode>

  Default value: `16` {{ "class": "mt-2 text-xs"}}

- **offsetY** - Vertical offset for the toaster's position. {{ "class" : "mb-2", "data-nav": "sublink", "id": "offsetY" }}

        <InlineCode lang="tsx">

            ```tsx
            offsetY: number;
            ```

        </InlineCode>

        Default value: `16` {{ "class": "mt-3 text-xs" }}

- **gutter** - Spacing between toasts. {{ "class" : "mb-2", "data-nav": "sublink", "id": "gutter" }}

        <InlineCode lang="tsx">

            ```tsx
            gutter: number;
            ```

        </InlineCode>

          Default value: `16` {{ "class": "mt-3 text-xs" }}

- **positionY** - Vertical positioning of the toaster. {{ "class" : "mb-2", "data-nav": "sublink", "id": "positionY" }}

        <InlineCode lang="tsx">

            ```tsx
            positionY: "top" | "bottom";
            ```

        </InlineCode>

        Default value: `"top"` {{ "class": "mt-3 text-xs" }}

- **positionX** - Horizontal positioning of the toaster. {{ "class" : "mb-2", "data-nav": "sublink", "id": "positionX" }}

        <InlineCode lang="tsx">

            ```tsx
            positionX: "left" | "center" | "right";
            ```

        </InlineCode>

          Default value: `"right"` {{ "class": "mt-3 text-xs" }}

- **renderOnWindowInactive** - If true, toasts will render even when the window is inactive (blurred). {{ "class" : "mb-2", "data-nav": "sublink", "id": "renderOnWindowInactive" }}

        <InlineCode lang="tsx">

            ```tsx
            renderOnWindowInactive: boolean;
            ```

        </InlineCode>

        Default value: `false` {{ "class": "mt-3 text-xs" }}

- **pauseOnWindowInactive** - If true, toast progress will pause when the window is inactive (blurred). {{ "class" : "mb-2", "data-nav": "sublink", "id": "pauseOnWindowInactive" }}

        <InlineCode lang="tsx">

            ```tsx
            pauseOnWindowInactive: boolean;
            ```

        </InlineCode>

           Default value: `true` {{ "class": "mt-3 text-xs" }}

- **toasterStyle** - Custom styles for the toaster container. {{ "class" : "mb-2", "data-nav": "sublink", "id": "toasterStyle" }}

        <InlineCode lang="tsx">

            ```tsx
            toasterStyle: Omit<JSX.CSSProperties, "top" | "bottom" | "left" | "right">;
            ```

        </InlineCode>

        Default value: `null` {{ "class": "mt-3 text-xs" }}

## Shared props {{ "data-nav": "link", "id": "shared-props" }}

Can be applied to all components; `ToastProvider`, `Toaster` and `Toast`.

- **theme** - The theme of the toast. Two theme types are included; `light` and `dark`, but any string can be provided. {{ "class": "mb-2", "data-nav": "sublink", "id": "theme" }}

    <InlineCode lang="tsx">

  ```tsx
  theme: string | undefined | null;
  ```

    </InlineCode>

      Default value: `"light"` {{ "class": "mt-3 text-xs" }}

- **type** - The type of the toast. Can be `default`, `success`, `error`, `info`, `warning` and `loading`. {{ "class": "mb-2", "data-nav": "sublink", "id": "type" }}

    <InlineCode lang="tsx">

  ```tsx
  type: ToastType;
  ```

    </InlineCode>

  Default value: `"default"` {{ "class": "mt-3 text-xs" }}

- **duration** - Duration (in milliseconds) before the toast is dismissed. Set to `false` or `0` for persistent toasts. {{ "class": "mb-2", "data-nav": "sublink", "id": "duration" }}

    <InlineCode lang="tsx">

  ```tsx
  duration: number | false;
  ```

    </InlineCode>

  Default value: `5000` {{ "class": "mt-3 text-xs" }}

- **onEnter** - CSS animation name when the toast enters. {{ "class": "mb-2", "data-nav": "sublink", "id": "onEnter" }}

    <InlineCode lang="tsx">

  ```tsx
  onEnter: string;
  ```

    </InlineCode>

  Default value: `undefined` {{ "class": "mt-3 text-xs" }}

- **enterDuration** - Duration of the enter animation in milliseconds. {{ "class": "mb-2", "data-nav": "sublink", "id": "enterDuration" }}

    <InlineCode lang="tsx">

  ```tsx
  enterDuration: number;
  ```

    </InlineCode>

  Default value: `300` {{ "class": "mt-3 text-xs" }}

- **onExit** - CSS animation name when the toast exits. {{ "class": "mb-2", "data-nav": "sublink", "id": "onExit" }}

    <InlineCode lang="tsx">

  ```tsx
  onExit: string;
  ```

    </InlineCode>

  Default value: `undefined` {{ "class": "mt-3 text-xs" }}

- **exitDuration** - Duration of the exit animation in milliseconds. {{ "class": "mb-2", "data-nav": "sublink", "id": "exitDuration" }}

    <InlineCode lang="tsx">

  ```tsx
  exitDuration: number;
  ```

    </InlineCode>

  Default value: `300` {{ "class": "mt-3 text-xs" }}

- **onIdle** - CSS class which will be applied when the toast is idle. {{ "class": "mb-2", "data-nav": "sublink", "id": "onIdle" }}

    <InlineCode lang="tsx">

  ```tsx
  onIdle: string;
  ```

    </InlineCode>
  
    Default value: `undefined` {{ "class": "mt-3 text-xs" }}

- **class** - CSS class applied to the toast body. Can be a string or a function returning a string. {{ "class": "mb-2", "data-nav": "sublink", "id": "class" }}

    <InlineCode lang="tsx">

  ```tsx
  class: string | ((args: ToastContext) => string);
  ```

    </InlineCode>

      Default value: `sn-toast` {{ "class": "mt-3 text-xs" }}

- **style** - Custom styles for the toast body. Can be an object or a function returning an object. {{ "class": "mb-2", "data-nav": "sublink", "id": "style" }}

    <InlineCode lang="tsx">

  ```tsx
  style: JSX.CSSProperties | ((args: ToastContext) => JSX.CSSProperties);
  ```

    </InlineCode>

       Default value: `undefined` {{ "class": "mt-3 text-xs" }}

More shared props will be documented in the next section...

## Special props: {{ "data-nav": "link", "id": "special-props" }}

- **id** - Unique identifier for the toast. If not provided, a random ID is generated. {{ "class": "mb-2", "data-nav": "sublink", "id": "id" }}

    <InlineCode lang="tsx">

  ```tsx
  id: string;
  ```

    </InlineCode>

- **content** - The main content of the toast. Can be a string, JSX element, or a function returning JSX. {{ "class": "mb-2", "data-nav": "sublink", "id": "content" }}

    <InlineCode lang="tsx">

  ```tsx
  content: ToastContent;
  ```

    </InlineCode>

- **contentType** - Determines whether the content is static or dynamic. {{ "class": "mb-2", "data-nav": "sublink", "id": "contentType" }}

    <InlineCode lang="tsx">

  ```tsx
  contentType: "static" | "dynamic";
  ```

    </InlineCode>

  Default value: `"static"` {{ "class": "mt-3 text-xs" }}

- **toasterId** - Specifies the toaster instance where the toast should be displayed. {{ "class": "mb-2", "data-nav": "sublink", "id": "toasterId" }}

    <InlineCode lang="tsx">

  ```tsx
  toasterId: string;
  ```

    </InlineCode>

Type: ToastContent (string | JSX.Element | (() => JSX.Element))

| **Property**             | **Type**                                                                        | **Description**                                                                                         | **Default**             |
| ------------------------ | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- | ----------------------- |
| `id`                     | `string`                                                                        | Unique identifier for the toast.                                                                        | Automatically generated |
| `content`                | `ToastContent`                                                                  | The content to display inside the toast.                                                                | `undefined`             |
| `contentType`            | `"static" \| "dynamic"`                                                         | Specifies whether the content is static or dynamically updated.                                         | `"static"`              |
| `theme`                  | `string \| undefined \| null`                                                   | Theme to apply to the toast.                                                                            | `undefined`             |
| `toasterId`              | `string`                                                                        | ID of the toaster instance the toast belongs to.                                                        | `undefined`             |
| `type`                   | `ToastType`                                                                     | Type of toast (e.g., success, error, info, etc.).                                                       | `undefined`             |
| `duration`               | `number \| false`                                                               | How long (in milliseconds) the toast should remain visible. Set `false` to disable automatic dismissal. | `5000`                  |
| `onEnter`                | `string \| undefined`                                                           | Custom animation class for the toast enter transition.                                                  | `undefined`             |
| `enterDuration`          | `number`                                                                        | Duration (in milliseconds) of the enter animation.                                                      | `300`                   |
| `onExit`                 | `string \| undefined`                                                           | Custom animation class for the toast exit transition.                                                   | `undefined`             |
| `exitDuration`           | `number`                                                                        | Duration (in milliseconds) of the exit animation.                                                       | `300`                   |
| `onIdle`                 | `string \| undefined`                                                           | Custom animation class for the idle state.                                                              | `undefined`             |
| `positionY`              | `"top" \| "bottom"`                                                             | Vertical position of the toast.                                                                         | `"top"`                 |
| `positionX`              | `"left" \| "right" \| "center"`                                                 | Horizontal position of the toast.                                                                       | `"center"`              |
| `offsetX`                | `number`                                                                        | Horizontal offset (in pixels) from the defined position.                                                | `0`                     |
| `offsetY`                | `number`                                                                        | Vertical offset (in pixels) from the defined position.                                                  | `0`                     |
| `gutter`                 | `number`                                                                        | Space (in pixels) between toasts in the stack.                                                          | `8`                     |
| `limit`                  | `number \| false`                                                               | Maximum number of toasts to display simultaneously. Set `false` for no limit.                           | `false`                 |
| `reverseToastOrder`      | `boolean`                                                                       | Whether to reverse the order of toasts in the stack.                                                    | `false`                 |
| `pauseOnHover`           | `boolean`                                                                       | Pause the timer when the user hovers over the toast.                                                    | `true`                  |
| `pauseOnWindowInactive`  | `boolean`                                                                       | Pause the timer when the browser window loses focus.                                                    | `true`                  |
| `renderOnWindowInactive` | `boolean`                                                                       | Whether to render the toast when the browser window is inactive.                                        | `true`                  |
| `class`                  | `string \| ((args: ToastContext) => string)`                                    | Custom CSS class for the toast. Accepts a string or a function returning a string.                      | `undefined`             |
| `style`                  | `JSX.CSSProperties \| ((args: ToastContext) => JSX.CSSProperties) \| undefined` | Inline styles for the toast. Accepts a style object or a function returning a style object.             | `undefined`             |
| `wrapperClass`           | `string \| ((args: ToastContext) => string)`                                    | Custom CSS class for the toast wrapper.                                                                 | `undefined`             |
| `wrapperStyle`           | `JSX.CSSProperties \| ((args: ToastContext) => JSX.CSSProperties) \| undefined` | Inline styles for the toast wrapper.                                                                    | `undefined`             |
| `enterCallback`          | `(() => void) \| null`                                                          | Callback function executed when the toast enters.                                                       | `null`                  |
| `updateCallback`         | `(() => void) \| null`                                                          | Callback function executed when the toast updates.                                                      | `null`                  |
| `exitCallback`           | `((reason?: boolean \| string) => void) \| null`                                | Callback function executed when the toast exits.                                                        | `null`                  |
| `showDismissButton`      | `boolean`                                                                       | Whether to show a dismiss button on the toast.                                                          | `true`                  |
| `dismissButtonClass`     | `string \| ((args: ToastContext) => string)`                                    | Custom CSS class for the dismiss button.                                                                | `undefined`             |
| `dismissButtonStyle`     | `JSX.CSSProperties \| ((args: ToastContext) => JSX.CSSProperties) \| undefined` | Inline styles for the dismiss button.                                                                   | `undefined`             |
| `dismissOnClick`         | `boolean`                                                                       | Whether clicking the toast dismisses it.                                                                | `true`                  |
| `toasterStyle`           | `Omit<JSX.CSSProperties, "top", "bottom", "left", "right", ...>`                | Inline styles for the toaster. Excludes certain properties like position-related styles.                | `null`                  |
| `showProgressBar`        | `boolean`                                                                       | Whether to show a progress bar on the toast.                                                            | `true`                  |
| `progressBarClass`       | `string \| ((args: ToastContext) => string)`                                    | Custom CSS class for the progress bar.                                                                  | `undefined`             |
| `progressBarStyle`       | `JSX.CSSProperties \| ((args: ToastContext) => JSX.CSSProperties) \| undefined` | Inline styles for the progress bar.                                                                     | `undefined`             |
| `showIcon`               | `boolean`                                                                       | Whether to show an icon in the toast.                                                                   | `true`                  |
| `icon`                   | `((args: ToastContext) => JSX.Element) \| JSX.Element \| null`                  | Custom icon for the toast. Accepts a JSX element or a function returning a JSX element.                 | `null`                  |
| `dragToDismiss`          | `boolean`                                                                       | Whether dragging the toast dismisses it.                                                                | `true`                  |
| `dragTreshold`           | `number`                                                                        | Distance (in pixels) required to dismiss the toast via dragging.                                        | `50`                    |
| `ariaLive`               | `"off" \| "assertive" \| "polite"`                                              | ARIA `live` attribute for screen readers.                                                               | `"polite"`              |
| `role`                   | `"status" \| "alert"`                                                           | ARIA `role` attribute for screen readers.                                                               | `"status"`              |
